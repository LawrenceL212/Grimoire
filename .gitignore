node_modules/
package*.json
*.png
_local.html
__pycache__/
.cache/
.DS_Store

# Authoring agents verify their arithmetic in sympy; those scratch scripts
# belong in the system temp directory, never here.

# Authoring agents verify their arithmetic with throwaway Python. Every script
# this project actually keeps lives in scripts/, so nothing at the top level is
# ours - naming them one at a time was losing the race.
/*.py
/*.mjs
