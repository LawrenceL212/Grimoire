{
 "id": "r",
 "name": "The Statistician's Hall",
 "subject": "R",
 "category": "language",
 "sigil": "📊",
 "unlock": null,
 "lang": "r",
 "runtime": "piston",
 "source": "exercism/r (MIT)",
 "importedBy": "scripts/import_exercism.py",
 "blurb": "R is an interpreted, open source programming language with powerful data manipulation and graphical capabilities. It has a thriving package ecosystem and is one of the most popular languages for data science.",
 "floors": [
  {
   "n": 1,
   "name": "Threshold of Syntax",
   "concepts": [
    "basics",
    "booleans"
   ],
   "exercises": [
    "lasagna",
    "annalyns-infiltration"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Variables and assignment",
      "body": "R is a dynamically typed language, so in general it is not necessary to specify the type of a variable. The preferred assignment operator is `<-` \n\nUsing `=` instead of `<-` will often but not always work, and is discouraged as poor style.\nIf working in RStudio, it can be entered with the keyboard shortcut `Alt-minus`.\n\nOperators are mostly conventional, but note `%/%` and `%%`:\n\nIn R, `#` starts a single-line comment.\nThere is no separate syntax for multi-line comments.",
      "code": "x <- 42\navogadro <- 6.02e23\nmystr <- \"Hello, World!\"",
      "lang": "r"
     },
     {
      "title": "Functions",
      "body": "To define a function:\n\nFunctions can have zero or more arguments, separated by commas.\nParentheses `()` are needed with `function()`, even if there are no parameters.\n\nDefault values can be specified for function arguments, though these must come after any arguments without defaults, and use `=` not `<-` to assign the default value.\nAs a rather pointless example:\n\nThe braces `{ }` are not strictly required for single-line function bodies, but including them is considered good style.\n\nR will automatically return the final value of a function, as in the examples above.\n\n`return()` can be used anywhere within the function body to make a return value explicit.\n\nCalling a function is the same as in many other languages:",
      "code": "squareit <- function(x) {\n    x * x\n}",
      "lang": "r"
     },
     {
      "title": "Booleans in R",
      "body": "True or false values are represented by the `logical` type.\nIt contains only two values: `TRUE` and `FALSE`.",
      "code": "TRUE\n#> [1] TRUE\nFALSE\n#> [1] FALSE\ntypeof(TRUE)\n#> [1] \"logical\"",
      "lang": "r"
     },
     {
      "title": "Boolean Operators",
      "body": "There are five Boolean operators in R, but only three are relevant at this stage of the syllabus.\nThe other two will be important when working with vectors, covered in several later Concepts.\n\n`&&` is Boolean \"and\".\nIt evaluates to `TRUE` if the expressions on *both* sides of `&&` are `TRUE`.\n\n`||` is Boolean \"or\".\nIt evaluates to `TRUE` if an expression on *either* side of `||` is `TRUE`.\n\n`!` is Boolean \"not\".\nIt exchanges `TRUE` and `FALSE` values.\n\nFor longer and more complicated expressions, it is best to use parentheses to make your intention clear.",
      "code": "TRUE && TRUE\n#> [1] TRUE\nTRUE && FALSE\n#> [1] FALSE",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "lasagna: tests not parsed (no extractor for 'r')",
    "annalyns-infiltration: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 2,
   "name": "Hall of Branching Paths",
   "concepts": [
    "conditionals",
    "vector-filtering",
    "vectors"
   ],
   "exercises": [
    "cars-assemble",
    "elyses-enchantments",
    "elyses-analytic-enchantments"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Comparison operators",
      "body": "R has a set of operators that is the same as many other languages:",
      "code": "1 == 2 # FALSE\n1 != 2 # TRUE\n1 < 2 # TRUE\n1 > 2 # FALSE\n1 <= 2 # TRUE\n1 >= 2 # FALSE",
      "lang": "r"
     },
     {
      "title": "Branching",
      "body": "The basic form of an if-else is \n\nIn this case, parentheses `()` around the boolean and braces `{}` around the statement are both required. \nA simple `if` statement can be shortened:",
      "code": "if (x > 10) {\n    y <- \"big\"\n} else if (x > 2) {\n    y <- \"medium\"\n} else {\n    y <- \"tiny\"\n}",
      "lang": "r"
     },
     {
      "title": "Creating vectors",
      "body": "If you already have all the values, use `c()` (short for \"concatenate\" or \"combine\"):\n\nThe `c()` function is very flexible, and inputs can include other vectors and ranges.\n`c()` will take any number and variety of inputs, and flatten them into a single vector.",
      "code": "x <- 10\nv <- c(4, 7, x)",
      "lang": "r"
     },
     {
      "title": "Ranges",
      "body": "For a range of consecutive values (increasing or decreasing) you can use `:` notation",
      "code": "v <- 1:5 # equivalent to c(1, 2, 3, 4, 5)\nw <- 10:7 # c(10, 9, 8, 7)",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "cars-assemble: tests not parsed (no extractor for 'r')",
    "elyses-enchantments: tests not parsed (no extractor for 'r')",
    "elyses-analytic-enchantments: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 3,
   "name": "The Bound Sigil",
   "concepts": [
    "set-operations",
    "strings",
    "switch"
   ],
   "exercises": [
    "ozans-playlist",
    "log-levels",
    "blackjack"
   ],
   "lesson": {
    "sections": [
     {
      "title": "R has no separate Set datatype, instead using a variety...",
      "body": "R has no separate Set datatype, instead using a variety of functions to perform similar operations on vectors.\n\nThe `%in%` operator tests for set membership.",
      "code": "2 %in% 1:10 # TRUE\n12 %in% 1:10 # FALSE",
      "lang": "r"
     },
     {
      "title": "Relevant functions include unique (to remove...",
      "body": "Relevant functions include `unique` (to remove duplicates), plus `union()`, `intersect()`, `setdiff()` and `setequal()` to operate on pairs of sets.",
      "code": "> set_1 <- c(\"a\", \"b\", \"c\", \"b\", \"a\")\n> unique(set_1) # deduplicate\n[1] \"a\" \"b\" \"c\"\n\n> set_2 <- c('a', \"c\", \"d\")\n> union(set_1, set_2) # values in either set\n[1] \"a\" \"b\" \"c\" \"d\"\n\n> intersect(set_1, set_2) # values in both sets\n[1] \"a\" \"c\"\n\n> setdiff(set_1, set_2) # values in set_1 but not set_2\n[1] \"b\"\n\n> setdiff(set_2, set_1) # values in set_2 but not set_1\n[1] \"d\"\n\nsetequal(set_1, c(\"c\", \"b\", \"a\", \"b\")) # are elements the same after deduplication?\n[1] TRUE",
      "lang": "r"
     },
     {
      "title": "Background",
      "body": "A string is a piece of text between quotes.\nAny currently-supported version of R encodes strings in UTF-8, so most world languages can be supported.\n\nUnfortunately, the terminology may be confusing to programmers familiar with other languages.\n\nAlthough the `type` is `character`, R makes no distinction between a single letter (called `char` in several other languages, but **not** in R) and a long string.\nBoth are a single item, and can be in single `' '` or double `\" \"` quotes interchangeably.\n\nThe style guide recommends using double quotes, except when you want to use double quotes within the string without escaping them.\n\nA `character vector` is thus a vector of strings, with each string as a single element.\n\nIn the above example, `length(s[1])` is equivalent to `length(\"some\")`: the number of **vector elements**, not the **string length**.\n\nManipulating an individual string is obviously possible, as with `nchar()` in the above example, but needs particular functions that will be discussed below.\n\n**String functions**\nMost things are **possible** in base R, but many programmers find support for string manipulation rather limited and confusing.\n\nFortunately, there are `tidyverse` packages that are much more intuitive, flexible and self-consistent.\n\n**Base R**\nThere is still a lot of legacy code using old-style string functions.\nThis includes most community solutions on Exercism.\n\nAdditionally, for small tasks it can be useful to use built-in functions instead of importing a large library.\nThe R track maintainers tend to take this approach when writing exemplar solutions to validate the exercises.\n\nTherefore, some knowledge of the old approaches is still worth learning.\n\n**`nchar`**\nReturns the number of characters in a string.\n\nThe (**complicated!**) topic of what \"character\" means is beyond our scope.\nJust note a couple of points in the examples above:\n\n- Though implemented in C internally, strings in R are not `\\0`-terminated (which would add 1 to the length).\n- Multi-byte Unicode characters are handled as you might expect/hope. \"Hrōðgār\" has 7 characters (also called \"runes\" or \"code points\"), but needs 10 bytes for storage.\n\n**`paste` and `paste0`**\nCombines an arbitrary number of strings into a single string.\n\nOptionally, a separator can be specified for `paste`: the default is a space.\n\n`paste0(v)` is equivalent to `paste(v, sep = \"\")`.\n\n**`sprintf`**\nFor finer control over string assembly, R copies the `sprintf` function from C (and several later languages: Wikipedia lists about 30).\n\n**`trimws`**\nLeading and/or trailing whitespace can be removed with `trimws()`:\n\nThere is a `which = \"left\"` (or \"right\") parameter to avoid trimming both ends.\n\n**`substr`**\nOperates on a single string and returns a substring between two (inclusive) limits.\n\n**`strsplit`**\nSplits a string into an R list, based on a separator string.\nThe separator is required (with no default), but can be an empty string.\n\nLists will be covered in a separate Concept.\nIf you have not already reached that part of the syllabus, just know for now that `unlist()` function will (in this case) convert a list to a vector.\n\n**Tidyverse**\nBeing charitable, we could say that the old-style string functions are not the best that R has to offer (**less charitable views are available**).\n\nTo improve the situation, the `stringr` library has been in development since 2009, and is now a key part of the Tidyverse collection.\n\nImport it into your namespace with `library(stringr)` or `library(tidyverse)`.\n\nSome key features include:\n\n- All function names have a `str_` prefix and try to make the purpose clear.\n- The order of arguments tries to be self-consistent, with input data as the first argument.\n- Inputs can range from a single string to complex data structures. Functions will try to adapt and do the most sensible thing.\n\nLike all the Tidyverse packages, `stringr` has good documentation, typically written by the package author(s).\n\n- A reference website\n- A cheatsheet\n- A chapter in \"R for Data Science\".\n\n**Diversion: A quick primer on pipes**\nThe use of pipes `|>` to connect functions will be discussed in more detail in other parts of the syllabus.\n\nFor now, it is useful to know that:\n\n- A result from the left of the pipe becomes the first argument in the function to the right (where \"right\" ignores optional line breaks).\n- Any remaining arguments can be included in the function call.\n- In R, we must pipe to a function **call**, not just a **name**, so parentheses are always required (**this differs from some other languages**).\n\nThe example below illustrates the syntax.\nIndividual functions will be discussed later.\n\n**Manage lengths**\n`str_length` counts code points (similar to `nchars`).\n\nVarious other functions manipulate whitespace, including `str_trim()` and `str_squish()`.\nThis is often a vital cleaning task at the start of any data science project.\n\n**Subset strings**\nThe function `str_sub(string, start, end)` will return the substring from `start` to `end` (inclusive), while `str_sub_all()` can operate on multiple strings.\n\nThere is a lot of flexibility.\n`start` and `end` default to the first and last character, respectively.\nThey  can be vectors of indices.\nIn welcome contrast to vector indexing, negative values count back from the string end, Python-style.\n\n**Join strings**\n`str_flatten` converts a character vector to a single string.\nThe `collapse` argument is inserted between each element and defaults to `\"\"`.\n\n`str_c` converts multiple character vectors to a single character vector, with recyling as necessary.\n\n`str_glue` brings simple string interpolation to R.\nExpressions inside braces `{ }` are interpreted and the text repesentation substituted.\n\n**Split strings**\nA very common use of R is to massage messy data into a consistent format for analysis.\nTo help this, there are several ways to split text into substrings.\nMost are beyond the current scope.\n\n`str_split_1` is the simplest, converting a single string into a vector of pieces, split at some specified pattern (there is no default pattern).\n\n**Mutate strings**\nSome languages (mainly European) distinguish between UPPERCASE and lowercase letters.\nThere are a set of `stringr` functions to interconvert these, in locale-appropriate ways:\n\n- `str_to_lower()`\n- `str_to_upper()`\n- `str_to_title()` : first letter of each word to upper\n- `str_to_sentence()` : first letter of sentence to upper\n\nFor substring replacement based on pattern matching, `str_replace()` changes the first occurrence of the pattern, `str_replace_all()` changes all occurrences.\n\nFor substring replacement based on character indexing, there is an assignment form of str_sub().",
      "code": "mystring <- \"some text\"\nmystring\n#> [1] \"some text\"\ntypeof(mystring)\n#> [1] \"character\"",
      "lang": "r"
     },
     {
      "title": "Functions in `dplyr`",
      "body": "The `switch` in base R is quite limited: it will only do an exact match to a single input.\nThis seemed reasonable when R was first released in 1993, but needs improvement for modern usage.\n\nAs mentioned in the `Conditionals` Concept, the `tidyverse` collection of packages is designed to supplement (and sometimes replace) base R functionality without impacting backwards compatibility.\n\nThe `dplyr` package can be brought into scope by adding either `library(dplyr)` (for the single package) or `library(tidyverse)` (for the whole collection) to the top of your code.\n\nThe `dplyr` library provides two extra functions related to `switch`.\n\n**The `recode_values` and `replace_values` functions**\nThese two related functions allow a vectorized switch-like mapping of old values to new values.\n\nThe main difference between them is that `recode_values()` creates an entirely new vector, while `replace_values()` allows partial updates of an existing vector.\n\nMatching is still exact, but:\n\n- The options on the left can be vectors, and the input matches if any element matches.\n- Options on the left and results on the right are linked with a tilde `~` instead of `=`.\n- A default can be specified in all cases.\n\n**The `case_when` function**\n`case_when` takes `case_match` syntax a stage further, by allowing any logical expression on the left of the `~`.\n\nThe input vector (`x` in the example below) is not supplied as an argument, it just needs to be already defined.\n\nThe `between()` function is also part of `dplyr`.",
      "code": "library(dplyr)\n\nx <- c(\"a\", \"b\", \"a\", \"d\", \"b\", NA, \"c\", \"e\", \"z\")\nrecode_values(\n  x,\n  \"a\" ~ 1,\n  \"b\" ~ 2,\n  \"c\" ~ 3,\n  c(\"d\", \"e\") ~ 4, # either \"d' or \"e\" will match\n  NA ~ 0,          # matches missing values\n  default = 100   # note the different syntax for the default\n)\n#> [1]   1   2   1   4   2   0   3   4 100",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "ozans-playlist: tests not parsed (no extractor for 'r')",
    "log-levels: tests not parsed (no extractor for 'r')",
    "blackjack: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 4,
   "name": "Vault of Collections",
   "concepts": [
    "loops",
    "names-attribute",
    "nothingness"
   ],
   "exercises": [
    "mixed-juices",
    "named-resistor-color",
    "name-badges"
   ],
   "lesson": {
    "sections": [
     {
      "title": "`while` and `repeat`",
      "body": "For open-ended problems where the number of times round the loop is not known in advance, R has the `while` loop with a condition, and the `repeat` loop with no condition (equivalent to `while (TRUE)`).\n\nThe basic form is fairly simple:\n\nIn this case, the program will keep going round the loop until `condition` is no longer `TRUE`.\n\nTwo ways to exit the loop early are available:\n\n- A `break` causes the loop to exit, with execution continuing at the next line after the loop's closing brace `}`.\n- A `return(x)` stops execution of the current function, passing the return value `x` back to the caller.\n\nWith these options available, it can sometimes be convenient to create an \"infinite\" loop with `repeat { ... }`, then rely on finding a stopping condition within the loop body to trigger a `break` or `return`.\n\nThese three variants are equivalent and all end with `x == 0.4444...`",
      "code": "while (condition) {\n  do_something()\n}",
      "lang": "r"
     },
     {
      "title": "Iterating over a vector",
      "body": "This is often unnecessary in R.\nAs discussed in other concepts, many functions will operate on entire vectors.\n\nHowever, explicit loops are sometimes unavoidable.\nThis is especially true when:\n\n- The loop body has side effects such as printing or file I/O.\n- Looping continues until some criterion is reached, after an unpredictable number of iterations.\n\nThe loop condition must be in parentheses.\nAs with function bodies, braces are optional (but often included) for single-line loop bodies.\n\nIf the current iteration fails to satisfy some condition, it is possible to skip immediately to the next iteration with a `next`:\n\nIf the numerical index is needed, use `seq_along()`.\n\nUsing `i in 1:length(v)` is **not** recommended, as it can cause problems when `length(v)` is zero.\nThe range `1:0` is equivalent to `c(1, 0)`, so the loop body will execute and probably fail.\nThe `seq_along()` function is designed to handle this case correctly.",
      "code": "words <- c(\"This\", \"is\", \"a\", \"loop\")\nfor (w in words) { print(w) } # the braces are optional here\n#> [1] \"This\"\n#> [1] \"is\"\n#> [1] \"a\"\n#> [1] \"loop\"",
      "lang": "r"
     },
     {
      "title": "Creation",
      "body": "There are three ways to add names to a vector.\n\n1) At vector creation time \n\n2) By assigning a character vector to `names()`\n\n3) With `setNames()`",
      "code": "> work_days <- c(Mon = TRUE, Tue = TRUE, Wed = TRUE, Thu = TRUE, Fri = TRUE, Sat = FALSE, Sun = FALSE)\n> work_days\n  Mon   Tue   Wed   Thu   Fri   Sat   Sun \n TRUE  TRUE  TRUE  TRUE  TRUE FALSE FALSE ",
      "lang": "r"
     },
     {
      "title": "Working with names",
      "body": "The `names()` function can retrieve names as well as set them.\n\nA name can be used in place of the position index, with quotes required in this case.\n\nFor such indexing to work correctly, it is best to ensure that names are unique and non-missing.\nHowever, R does not enforce uniqueness.\n\nThe usual vector operations still work, and names usually will be preserved if that makes sense.",
      "code": "> names(months) <- month.abb\n> names(months)[1:3]\n[1] \"Jan\" \"Feb\" \"Mar\"",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "mixed-juices: tests not parsed (no extractor for 'r')",
    "named-resistor-color: tests not parsed (no extractor for 'r')",
    "name-badges: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 5,
   "name": "Chamber of Forms",
   "concepts": [
    "functions",
    "lists",
    "vector-functions"
   ],
   "exercises": [
    "need-for-speed",
    "bird-watcher",
    "coordinate-transformation"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Creating lists",
      "body": "Giving names (often called \"tags\") to the elements is more common with lists than with vectors:\n\nHowever, tags are optional and R will then just use index numbers:",
      "code": "list(x = 5.3, y = 4.2, color = \"blue\")\n$x\n#> [1] 5.3\n\n$y\n#> [1] 4.2\n\n$color\n#> [1] \"blue\"",
      "lang": "r"
     },
     {
      "title": "Accessing list elements",
      "body": "There are three ways to do this, each useful in different contexts.\n\n**1. With `\\$tag` notation**\nVery common if the tag names are known in advance.\n\n**2. With `[[tag]]` notation**\nThis has the advantage that the tag name can be chosen at runtime.\nNote the double brackets.\n\n**3. With position index**\nThis works whether or not the list has tags.\nAgain, brackets are doubled.",
      "code": "point <- list(x = 5.3, y = 4.2, color = \"blue\")\npoint$x\n#> [1] 5.3\npoint$color\n#> [1] \"blue\"",
      "lang": "r"
     },
     {
      "title": "Vector-in, scalar-out",
      "body": "We already saw functions that take in a vector and return a single, scalar-like value:\n\nMany statistical functions are also in this category, such as `mean()`.\n\nFor logical vectors, there are `all()` and `any()`.",
      "code": "> v <- 1:5\n> length(v)\n#> [1] 5\n> sum(v)\n#> [1] 15",
      "lang": "r"
     },
     {
      "title": "Vector-in, vector-out",
      "body": "Many functions in R will operate on entire vectors, often giving vector output.\nThis includes most of the mathematical functions:\n\nThis is not just concise and convenient, avoiding the need for loops, list comprehensions or recursion, which are common in other languages.\nIn R, vector functions often run much faster than those techniques.\n\nFor sorting a vector, there is `sort()` to return the values and `order()` to return the indices:\n\nThe default is to sort in ascending order.\nSpecify `decreasing = TRUE` if necessary, or use `rev()` to reverse an existing vector.\n\nThere are also functions to produce cumulative vector outputs, operating on the input vector left-to-right:",
      "code": "> sq <- c(4, 9, 16, 25)\n> sqrt(sq) # square root\n#> [1] 2 3 4 5",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "need-for-speed: tests not parsed (no extractor for 'r')",
    "bird-watcher: tests not parsed (no extractor for 'r')",
    "coordinate-transformation: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 6,
   "name": "The Iterating Spiral",
   "concepts": [
    "dates-times",
    "errors",
    "functional-programming"
   ],
   "exercises": [
    "factory-sensors",
    "cheese-club",
    "booking-up-for-beauty"
   ],
   "lesson": {
    "sections": [
     {
      "title": "`stop()`",
      "body": "Some problems are too fundamental to let the program continue. \nIn this case, use `stop(msg)` to immediately halt the program with an `Error` and print `msg`.",
      "code": "f <- function() {\n    stop(\"I have a problem\")\n}\nf()\n#> Error in f() : I have a problem",
      "lang": "r"
     },
     {
      "title": "`stopifnot()`",
      "body": "Similar to an `assert` in other languages, this is a convenient wrapper for `stop()`. \nThe parameters are an arbitrary number of comma-separated boolean conditions, and the program will only continue if they are all `TRUE`.",
      "code": "stopifnot(1 < 2) # continues OK\nstopifnot(1 < 2, 1 == 2)\n#> Error: 1 == 2 is not TRUE",
      "lang": "r"
     },
     {
      "title": "Pipes",
      "body": "The use of pipes `|>` to connect functions is an increasingly common technique in several languages.\n\nIn brief:\n\n- A result from the left of the pipe becomes the first argument in the function to the right (where \"right\" ignores optional line breaks).\n- Any remaining arguments can be included in the function call.\n- In R, we must pipe to a function **call**, not just a **name**, so parentheses are always required (**this differs from some other languages**).\n\nSome programmers find it helpful to pronounce the pipe as \"then\" when reviewing code: do step 1 THEN do step 2...\n\nThe example below illustrates the syntax.\n\nThis type of pipe was added to Base R in v4.1.0, and you may see it referred to as the \"native pipe\".\n\nThe RStudio editor (among others) uses `Ctrl-Shift-M` as a keyboard shortcut to add a pipe with suitable spaces.",
      "code": "library(stringr)\n\n\"Monday-25-February\" |> \n  str_split(\"-\") |>     \n  unlist() |>               # [1] \"Monday\"   \"25\"       \"February\"\n  str_sub(1, 3) |>          # [1] \"Mon\" \"25\"  \"Feb\"\n  str_to_upper() |>         # [1] \"MON\" \"25\"  \"FEB\"\n  str_flatten(collapse = \", \")\n#> [1] \"MON, 25, FEB\"",
      "lang": "r"
     },
     {
      "title": "Apply functions",
      "body": "We saw previously that many functions in R are \"vectorized\": they will operate across entire vectors, with no need to write explicit loops.\n\nFunctions using only vectorized functions are themselves likely to be automatically vectorized.\nIn a trivial example:\n\nHowever, more complex functions may not accept vector input, for example if they contain loops or if-else blocks.\n\nWe need a more general way to apply functions to vectors (similar to `map()` in some other languages).\n\nBecause R has a rich variety of data structures in the base language, it also has a whole family of `*apply()` functions to operate on them.\nMore technically, these are often called \"higher-order functions\" by programmers and \"functionals\" by mathematicians, because they take functions as arguments.\n\nFor now, consider `lapply()` and `sapply()`.\n\n**`lapply()`**\n\"List-apply\" is designed to work on list inputs, but vectors will be silently coerced to lists as necessary.\n\nThe output is a list, so this may need unpacking to get a vector:\n\nStudents familiar with the Switch Concept may notice that we could alternatively replace scalar `switch` with vectorized `recode_values()` from the `dplyr` library.\nVectorization is good, and modern R provides multiple ways to achieve it.\n\n**`sapply()`**\n\"Simplified-apply\" will do something similar to `lapply`, but also tries to simplify the return value: for example,\n a named vector instead of a list.\nDepending on context, use of `unname()` may be necessary:",
      "code": "triple <- function(x) return(3 * x)\ntriple(1:10)\n [1]  3  6  9 12 15 18 21 24 27 30\n ",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "factory-sensors: tests not parsed (no extractor for 'r')",
    "cheese-club: tests not parsed (no extractor for 'r')",
    "booking-up-for-beauty: tests not parsed (no extractor for 'r')"
   ]
  },
  {
   "n": 7,
   "name": "Sanctum of Structure",
   "concepts": [
    "dataframes",
    "randomness",
    "regular-expressions"
   ],
   "exercises": [
    "captains-log",
    "regular-chatbot",
    "trees-and-tibbleations"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Discrete values with `sample`",
      "body": "To pick from a limited number of discrete values, `sample()` can be used with or without replacement.\nThis is useful for integers, strings, etc.\n\nSampling fits well into data pipelines.\nFor example, to generate random alphanumneric strings:",
      "code": "sample(1:10, 5) # no repeats by default\n#> [1] 8 3 4 2 6\n\nsample(1:10, 5, replace = TRUE) # repeats are OK\n#> [1] 10 10  7 10  9\n\nlanguages <- c(\"Fortran\", \"R\", \"Python\", \"Julia\")\nsample(languages, 1)\n#> [1] \"Python\"\n\nsample(c(\"H\", \"T\"), 5, replace = TRUE) # coin flips\n#> [1] \"T\" \"T\" \"H\" \"H\" \"H\"\n\nsample(c(TRUE, FALSE), 5, replace = TRUE) # alternative coin flips\n#> [1] FALSE  TRUE FALSE  TRUE  TRUE",
      "lang": "r"
     },
     {
      "title": "Shuffling",
      "body": "Several other languages have a special function to shuffle a sequence.\n\nR has no shuffle function.\n*Why?*\n\nTo understand this, look again at the `sample` function.\n\nThe full form is `sample(x, size, replace = FALSE, prob = NULL)`, where `x` is (usually) the sequence to sample from and `size` is the number of items to choose.\n\nWe already discussed `replace`, where the default is sampling without replacement.\nFor now, ignore `prob`, which allows you to give unequal weights to the various elements in `x`.\n\nWhat happens if we omit `size` and just provide `x`?\n\nIn typical R style, the pattern is vector-in, vector-out, and `size` defaults to `length(x)`.\n\n\"Sample all elements of `x` without replacement\" is just a more technical way to say \"shuffle `x`\".\n\n*Thus*, there is no `shuffle` function, because `sample` already does exactly what we need!",
      "code": "x <- letters[1:6]\nx\n#> [1] \"a\" \"b\" \"c\" \"d\" \"e\" \"f\"\nsample(x)\n#> [1] \"e\" \"a\" \"f\" \"b\" \"c\" \"d\"",
      "lang": "r"
     },
     {
      "title": "Detecting matches",
      "body": "Here are some questions, and functions that can help answer them.\n\nSome examples use `fruit`, a vector of 80 fruit names included with `stringr` for practicing.\n\n**Does a string contain a match?**\n`str_detect()`, or `str_starts()`, `str_ends()` to avoid using the `^` or `\\$` anchors.\n\n**Return only strings containing a match?**\nUse `str_subset()` to get the strings, `str_which()` to get the indices.\n\n**How many matches in each string?**\nUse `str_count()`.\n\n**Where is the match?**\n`str_view()` for interactive use, `str_locate()` or `str_locate_all()` more generally.",
      "code": "# berries begining with b\nfruit |> str_detect(\"^b.*berry\") |> head(15)\n#> [1] FALSE FALSE FALSE FALSE FALSE  TRUE  TRUE FALSE FALSE  TRUE  TRUE FALSE FALSE FALSE FALSE\n\n# alternative syntax\nfruit |> str_starts(\"b.*berry\") |> head(15)\n#> [1] FALSE FALSE FALSE FALSE FALSE  TRUE  TRUE FALSE FALSE  TRUE  TRUE FALSE FALSE FALSE FALSE",
      "lang": "r"
     },
     {
      "title": "Captures",
      "body": "Commonly, we want to know **what** matches. \nThis is achieved by including capture groups in parentheses within the regex.\n\nIt is possible to refer back to captures within the same regex, using `\\1`, `\\2`, etc.\nFor example, finding repeated pairs of letters:\n\nWith similar syntax, captures can be used in replacement strings:\n\nGather matches for later processing using the `str_match()` function for just the first match, or `str_match_all()` for all matches.\n\nCapture groups tend to produce complicated output in any language.\nThe R approach (**which is also vectorized as an added challenge**) is to generate a matrix from `str_match()`, with one row per input string, column 1 containing the full matched string, remaining columns containing the various capture groups.\n\nUsing named groups can be an advantage, as they become column names in the results matrix.\n\nWe have not said much about matrices in this syllabus, though there is a concept document you can access.\nMatrices do not fit well with the Tidyverse ecosystem, despite being part of Base R for a long time.\n\nIn quick summary: treat matrices like vectors with 2 indices, in the order `[rows, cols]`.\nLeave either index blank to get the whole row/column.\n\nWhen getting a single row `r` with `m[r,]` or a single column `c` with `m[,c]`, the value returned is flattened to a vector by default.\n\nInevitably, `str_match_all()` produces output with even more structure:\n\n- The result is a list, with one element per input string.\n- If there is only one input string, unlist the output with `[[1]]` indexing as described in the Lists Concept.\n- Each element is a matrix, with columns as for `match()`.\n- Each row in the matrix is a successful capture.\n\nOf course, matches can fail.\nThe result will then be the special value `NA`.\nBe ready to test for this, as described in the Nothingness Concept.",
      "code": "fruit |> str_subset(\"(..)\\\\1\")\n[1] \"banana\"      \"coconut\"     \"cucumber\"    \"jujube\"      \"papaya\"      \"salal berry\"",
      "lang": "r"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "captains-log: tests not parsed (no extractor for 'r')",
    "regular-chatbot: tests not parsed (no extractor for 'r')",
    "trees-and-tibbleations: tests not parsed (no extractor for 'r')"
   ]
  }
 ]
}