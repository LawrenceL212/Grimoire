{
 "id": "go",
 "name": "The Concurrent Warrens",
 "subject": "Go",
 "category": "language",
 "sigil": "🐹",
 "unlock": null,
 "lang": "go",
 "runtime": "piston",
 "source": "exercism/go (MIT)",
 "importedBy": "scripts/import_exercism.py",
 "blurb": "Go is a compiled, open source programming language with a small, consistent syntax, a powerful standard library, and fantastic tooling. It's a great fit for web backends and command-line tools.",
 "floors": [
  {
   "n": 1,
   "name": "Threshold of Syntax",
   "concepts": [
    "arithmetic-operators",
    "basics",
    "booleans",
    "numbers"
   ],
   "exercises": [
    "lasagna",
    "annalyns-infiltration",
    "cars-assemble"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Packages",
      "body": "Go applications are organized in packages.\nA package is a collection of source files located in the same directory.\nAll source files in a directory must share the same package name.\nWhen a package is imported, only entities (functions, types, variables, constants) whose names start with a capital letter can be used / accessed.\nThe recommended style of naming in Go is that identifiers will be named using `camelCase`, except for those meant to be accessible across packages which should be `PascalCase`.",
      "code": "package lasagna",
      "lang": "go"
     },
     {
      "title": "Variables",
      "body": "Go is statically-typed, which means all variables must have a defined type at compile-time.\n\nVariables can be defined by explicitly specifying a type:\n\nYou can also use an initializer, and the compiler will assign the variable type to match the type of the initializer.\n\nOnce declared, variables can be assigned values using the `=` operator.\nOnce declared, a variable's type can never change.",
      "code": "var explicit int // Explicitly typed",
      "lang": "go"
     },
     {
      "title": "Booleans in Go are represented by the predeclared...",
      "body": "Booleans in Go are represented by the predeclared boolean type `bool`, which values can be either `true` or `false`.\nIt's a defined type.",
      "code": "var closed bool    // boolean variable 'closed' implicitly initialized with 'false'\nspeeding := true   // boolean variable 'speeding' initialized with 'true'\nhasError := false  // boolean variable 'hasError' initialized with 'false' ",
      "lang": "go"
     },
     {
      "title": "Converting between types",
      "body": "Converting between types is done via a function with the name of the type to convert to.\nFor example:",
      "code": "var x int = 42 // x has type int\nf := float64(x) // f has type float64 (ie. 42.0)\nvar y float64 = 11.9 // y has type float64\ni := int(y) // i has type int (ie. 11)",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "lasagna: tests not parsed (no extractor for 'go')",
    "annalyns-infiltration: tests not parsed (no extractor for 'go')",
    "cars-assemble: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 2,
   "name": "Hall of Branching Paths",
   "concepts": [
    "comments",
    "packages",
    "string-formatting",
    "strings",
    "strings-package"
   ],
   "exercises": [
    "welcome-to-tech-palace",
    "party-robot",
    "weather-forecast"
   ],
   "lesson": {
    "sections": [
     {
      "title": "A string in Go is an immutable sequence of bytes, which...",
      "body": "A `string` in Go is an immutable sequence of bytes, which don't necessarily have to represent characters.\n\nA string literal is defined between double quotes:",
      "code": "const name = \"Jane\"",
      "lang": "go"
     },
     {
      "title": "Strings can be concatenated via the + operator:",
      "body": "Strings can be concatenated via the `+` operator:",
      "code": "\"Jane\" + \" \" + \"Austen\"\n// => \"Jane Austen\"",
      "lang": "go"
     },
     {
      "title": "Packages",
      "body": "In Go an application is organized in packages.\nA package is a collection of source files located in the same folder.\nAll source files in a folder must have the same package name at the top of the file.\nBy convention packages are named to be the same as the folder they are located in.\n\nGo provides an extensive standard library of packages which you can use in your program using the `import` keyword.\nStandard library packages are imported using their name.\n\nAn imported package is then addressed with the package name:\n\nGo determines if an item can be called by code in other packages through how it is declared.\nTo make a function, type, variable, constant or struct field externally visible (known as **exported**) the name must start with a capital letter.",
      "code": "package greeting",
      "lang": "go"
     },
     {
      "title": "String Formatting",
      "body": "Go provides an in-built package called `fmt` (format package) which offers a variety of functions to manipulate the format of input and output.\nThe most commonly used function is `Sprintf`, which uses verbs like `%s` to interpolate values into a string and returns that string.\n\nIn Go floating point values are conveniently formatted with Sprintf's verbs: `%g` (compact representation), `%e` (exponent) or `%f` (non exponent).\nAll three verbs allow the field's width and numeric position to be controlled.\n\nYou can find a full list of available verbs in the format package documentation.\n\n`fmt` contains other functions for working with strings, such as `Println` which simply prints the arguments it receives to the console and `Printf` which formats the input in the same way as `Sprintf` before printing it.",
      "code": "import \"fmt\"\n\nfood := \"taco\"\nfmt.Sprintf(\"Bring me a %s\", food)\n// Returns: Bring me a taco",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "welcome-to-tech-palace: tests not parsed (no extractor for 'go')",
    "party-robot: tests not parsed (no extractor for 'go')",
    "weather-forecast: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 3,
   "name": "The Bound Sigil",
   "concepts": [
    "comparison",
    "conditionals-if",
    "conditionals-switch",
    "slices",
    "variadic-functions"
   ],
   "exercises": [
    "vehicle-purchase",
    "blackjack",
    "card-tricks"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Comparison",
      "body": "In Go numbers can be compared using the following relational and equality operators.\n\nThe result of the comparison is always a boolean value, so either `true` or `false`.\n\nThe comparison operators above can also be used to compare strings.\nIn that case a lexicographical (dictionary) order is applied.\nFor example:",
      "code": "a := 3\n\na != 4 // true\na > 5  // false",
      "lang": "go"
     },
     {
      "title": "If Statements",
      "body": "Conditionals in Go are similar to conditionals in other languages.\nThe underlying type of any conditional operation is the `bool` type, which can have the value of `true` or `false`.\nConditionals are often used as flow control mechanisms to check for various conditions.\n\nFor checking a particular case an `if` statement can be used, which executes its code if the underlying condition is `true` like this:\n\nIn scenarios involving more than one case many `if` statements can be chained together using the `else if` and `else` statements.\n\nIf statements can also include a short initialization statement that can be used to initialize one or more variables for the if statement.\nFor example:\nNote: any variables created in the initialization statement go out of scope after the end of the if statement.",
      "code": "var value string\n\nif value == \"val\" {\n    return \"was val\"\n}",
      "lang": "go"
     },
     {
      "title": "Like other languages, Go also provides a switch statement.",
      "body": "Like other languages, Go also provides a `switch` statement.\nSwitch statements are a shorter way to write long `if ... else if` statements.\nTo make a switch, we start by using the keyword `switch` followed by a value or expression.\nWe then declare each one of the conditions with the `case` keyword.\nWe can also declare a `default` case, that will run when none of the previous `case` conditions matched:",
      "code": "operatingSystem := \"windows\"\n\nswitch operatingSystem {\ncase \"windows\":\n    // do something if the operating system is windows\ncase \"linux\":\n    // do something if the operating system is linux\ncase \"macos\":\n    // do something if the operating system is macos\ndefault:\n    // do something if the operating system is none of the above\n} ",
      "lang": "go"
     },
     {
      "title": "One interesting thing about switch statements, is that...",
      "body": "One interesting thing about switch statements, is that the value after the `switch` keyword can be omitted, and we can have boolean conditions for each `case`:",
      "code": "age := 21\n\nswitch {\ncase age > 20 && age < 30:\n    // do something if age is between 20 and 30\ncase age == 10:\n    // do something if age is equal to 10\ndefault:\n    // do something else for every other case\n}",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "vehicle-purchase: tests not parsed (no extractor for 'go')",
    "blackjack: tests not parsed (no extractor for 'go')",
    "card-tricks: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 4,
   "name": "Vault of Collections",
   "concepts": [
    "for-loops",
    "functions",
    "maps"
   ],
   "exercises": [
    "bird-watcher",
    "lasagna-master",
    "gross-store"
   ],
   "lesson": {
    "sections": [
     {
      "title": "General syntax",
      "body": "The for loop is one of the most commonly used statements to repeatedly execute some logic.\nIn Go it consists of the `for` keyword, a header and a code block that contains the body of the loop wrapped in curly brackets.\nThe header consists of 3 components separated by semicolons `;`: init, condition and post.\n\n- The **init** component is some code that runs only once before the loop starts.\n- The **condition** component must be some expression that evaluates to a boolean and controls when the loop should stop.\n  The code inside the loop will run as long as this condition evaluates to true.\n  As soon as this expression evaluates to false, no more iterations of the loop will run.\n- The **post** component is some code that will run at the end of each iteration.\n\n**Note:** Unlike other languages, there are no parentheses `()` surrounding the three components of the header.\nIn fact, inserting such parenthesis is a compilation error.\nHowever, the braces `{ }` surrounding the loop body are always required.",
      "code": "for init; condition; post {\n  // loop body - code that is executed repeatedly as long as the condition is true\n}",
      "lang": "go"
     },
     {
      "title": "For Loops - An example",
      "body": "The init component usually sets up a counter variable, the condition checks whether the loop should be continued or stopped and the post component usually increments the counter at the end of each repetition.\n\nThis loop will print the numbers from `1` to `9` (including `9`).\nDefining the step is often done using an increment or decrement statement, as shown in the example above.",
      "code": "for i := 1; i < 10; i++ {\n  fmt.Println(i)\n}",
      "lang": "go"
     },
     {
      "title": "Function Parameters",
      "body": "All parameters must be explicitly typed; there is no type inference for parameters.\nThere are no default values for parameters so all function parameters are required.\n\nParameters of the same type can be declared together, followed by a single type declaration.",
      "code": "import \"fmt\"\n\n// No parameters\nfunc PrintHello() {\n    fmt.Println(\"Hello\")\n}\n\n// Two parameters\nfunc PrintGreetingName(greeting string, name string) {\n  fmt.Println(greeting + \" \" + name)\n}",
      "lang": "go"
     },
     {
      "title": "Parameters vs Arguments",
      "body": "Let's quickly cover two terms that are often confused together: `parameters` and `arguments`.\nFunction parameters are the names defined in the function's signature, such as `greeting` and `name` in the function `PrintGreetingName` above.\nFunction arguments are the concrete values passed to the function parameters when we invoke the function.\nFor instance, in the example below, `\"Hello\"` and `\"Katrina\"` are the arguments passed to the `greeting` and `name` parameters:",
      "code": "PrintGreetingName(\"Hello\", \"Katrina\")",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "bird-watcher: tests not parsed (no extractor for 'go')",
    "lasagna-master: tests not parsed (no extractor for 'go')",
    "gross-store: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 5,
   "name": "Chamber of Forms",
   "concepts": [
    "constants",
    "range-iteration",
    "structs",
    "type-definitions"
   ],
   "exercises": [
    "need-for-speed",
    "chessboard",
    "savings-account"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Defining a struct",
      "body": "You create a new struct by using the `type` and `struct` keywords, and explicitly define the name and type of the fields.\nFor example, here we define `Shape` struct, that holds the name of a shape and its size:\n\nField names in structs follow the Go convention - fields whose name starts with a lower case letter are only visible to code in the same package, whereas those whose name starts with an upper case letter are visible in other packages.",
      "code": "type Shape struct {\n    name string\n    size int\n}",
      "lang": "go"
     },
     {
      "title": "Creating instances of a struct",
      "body": "Once you have defined the `struct`, you need to create a new instance defining the fields using their field name\nin any order:\n\nTo read or modify instance fields, use the `.` notation:\n\nFields that don't have an initial value assigned, will have their zero value.\nFor example:\n\nYou can create an instance of a `struct` without using the field names, as long as you define the fields **in order**:\n\nHowever, this syntax is considered **brittle code** since it can break when a field is added, especially when the new field is of a different type.\nIn the following example we add an extra field to `Shape`:",
      "code": "s := Shape {\n    name: \"Square\",\n    size: 25,\n}",
      "lang": "go"
     },
     {
      "title": "Iterate over a slice",
      "body": "Easy as pie, loops over a slice, ordered as expected.",
      "code": "xi := []int{10, 20, 30}\nfor i, x := range xi {\n  fmt.Println(i, x)\n}\n// outputs:\n// 0, 10\n// 1, 20\n// 2, 30",
      "lang": "go"
     },
     {
      "title": "Iterate over a map",
      "body": "Iterating over a map raises a new problem.\nThe order is now random.\n\nIt may seem the above output is incorrect, as one would expect the first key/value pair on the declaration of the map `9 10` to be the first one printed and not the last.\nHowever, maps are unordered by nature - there isn't a first or last key/value pair.\nBecause of that, when iterating over the entries of a map, the order by which entries will be visited will be random and not follow any specific pattern.\nThis means the above output is possible but might differ from what you get if you try to run this yourself.\nTo learn more about this see Go Language Spec: range clause.",
      "code": "hash := map[int]int{9: 10, 99: 20, 999: 30}\nfor k, v := range hash {\n  fmt.Println(k, v)\n}\n// outputs, for example:\n// 99 20\n// 999 30\n// 9 10",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "need-for-speed: tests not parsed (no extractor for 'go')",
    "chessboard: tests not parsed (no extractor for 'go')",
    "savings-account: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 6,
   "name": "The Iterating Spiral",
   "concepts": [
    "methods",
    "pointers",
    "time"
   ],
   "exercises": [
    "booking-up-for-beauty",
    "election-day",
    "jedliks-toys"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Layout Options",
      "body": "For a custom layout use combination of these options.\nIn Go predefined date and timestamp format constants are also available.\n\nThe `time.Time` type has various methods for accessing a particular time. e.g. Hour: `Time.Hour()`, Month: `Time.Month()`.\nMore on how this works can be found in official documentation.\n\nThe `time` includes another type, `Duration`, representing elapsed time, plus support for locations/time zones, timers, and other related functionality that will be covered in another concept.",
      "code": "package booking\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\n// Schedule returns a time.Time from a string containing a date.\nfunc Schedule(date string) time.Time {\n\tt, _ := time.Parse(\"1/2/2006 15:04:05\", date)\n\treturn t\n}\n\n// HasPassed returns whether a date has passed.\nfunc HasPassed(date string) bool {\n\tt, _ := time.Parse(\"January 2, 2006 15:04:05\", date)\n\treturn t.Before(time.Now())\n}\n\n// IsAfternoonAppointme",
      "lang": "go"
     },
     {
      "title": "Variables and Memory",
      "body": "Let's say we have a regular integer variable `a`:\n\nWhen we declare a variable, Go has to find a place in memory to store its value.\nThis is largely abstracted from us — when we need to fetch the value stored in that piece of memory, we can just refer to it by the variable name.\n\nFor instance, when we write `a + 2`, we are effectively fetching the value stored in the memory associated with the variable `a` and adding 2 to it.\n\nSimilarly, when we need to change the value in the piece of memory of `a`, we can use the variable name to do an assignment:\n\nThe piece of memory that is associated with `a` will now be storing the value `3`.",
      "code": "var a int",
      "lang": "go"
     },
     {
      "title": "Pointers",
      "body": "While variables allow us to refer to values in memory, sometimes it's useful to know the **memory address** to which the variable is pointing. **Pointers** hold the memory addresses of those values.\nYou declare a variable with a pointer type by prefixing the underlying type with an asterisk:\n\nHere we declare a variable `p` of type \"pointer to int\" (`*int`).\nThis means that `p` will hold the memory address of an integer.\nThe zero value of pointers is `nil` because a `nil` pointer holds no memory address.\n\n**Getting a pointer to a variable**\nTo find the memory address of the value of a variable, we can use the `&` operator.\nFor example, if we want to find and store the memory address of variable `a` in the pointer `p`, we can do the following:\n\n**Accessing the value via a pointer (dereferencing)**\nWhen we have a pointer, we might want to know the value stored in the memory address the pointer represents.\nWe can do this using the `*` operator:\n\nThe operation `*p` fetches the value stored at the memory address stored in `p`.\nThis operation is often called \"dereferencing\".\n\nWe can also use the dereference operator to assign a new value to the memory address referenced by the pointer:\n\nAssigning to `*pa` will change the value stored at the memory address `pa` holds.\nSince `pa` holds the memory address of `a`, by assigning to `*pa` we are effectively changing the value of `a`!\n\nA note of caution however: always check if a pointer is not `nil` before dereferencing.\nDereferencing a `nil` pointer will make the program crash at runtime!\n\n**Pointers to structs**\nSo far we've only seen pointers to primitive values.\nWe can also create pointers for structs:\n\nWe could have also created a new `Person` and immediately stored a pointer to it:\n\nWhen we have a pointer to a struct, we don't need to dereference the pointer before accessing one of the fields:",
      "code": "var p *int // 'p' contains the memory address of an integer",
      "lang": "go"
     },
     {
      "title": "A method is a function with a special receiver argument.",
      "body": "A method is a function with a special **receiver** argument.\nThe receiver appears in its own argument list between `func` keyword and the name of the method.",
      "code": "func (receiver type) MethodName(parameters) (returnTypes) {\n}",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "booking-up-for-beauty: tests not parsed (no extractor for 'go')",
    "election-day: tests not parsed (no extractor for 'go')",
    "jedliks-toys: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 7,
   "name": "Sanctum of Structure",
   "concepts": [
    "interfaces",
    "runes",
    "type-assertion",
    "type-conversion"
   ],
   "exercises": [
    "logs-logs-logs",
    "airport-robot",
    "sorting-room"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Using Runes",
      "body": "Variables of type `rune` are declared by placing a character inside single quotes:\n\nSince `rune` is just an alias for `int32`, printing a rune's type will yield `int32`:\n\nSimilarly, printing a rune's value will yield its integer (decimal) value:\n\nTo print the Unicode character represented by the rune, use the `%c` formatting verb:\n\nTo print the Unicode code point represented by the rune, use the `%U` formatting verb:",
      "code": "myRune := '¿'",
      "lang": "go"
     },
     {
      "title": "Runes and Strings",
      "body": "Strings in Go are encoded using UTF-8 which means they contain Unicode characters.\nCharacters in strings are stored and encoded as 1, 2, 3, or 4 bytes depending on the Unicode character they represent.\n\nIn Go, slices are used to represent sequences and these slices can be iterated over using range.\nWhen we iterate over a string, Go converts the string into a series of Runes, each of which is 4 bytes (remember, the rune type is an alias for an `int32`!)\n\nEven though a string is just a slice of bytes, the `range` keyword iterates over a string's runes, not its bytes.\n\nIn this example, the `index` variable represents the starting index of the current rune's byte sequence and the `char` variable represents the current rune:\n\nSince runes can be stored as 1, 2, 3, or 4 bytes, the length of a string may not always equal the number of characters in the string.\nUse the builtin `len` function to get the length of a string in bytes and the `utf8.RuneCountInString` function to get the number of runes in a string:",
      "code": "myString := \"❗hello\"\nfor index, char := range myString {\n  fmt.Printf(\"Index: %d\\tCharacter: %c\\t\\tCode Point: %U\\n\", index, char, char)\n}\n// Output:\n// Index: 0\tCharacter: ❗\t\tCode Point: U+2757\n// Index: 3\tCharacter: h\t\tCode Point: U+0068\n// Index: 4\tCharacter: e\t\tCode Point: U+0065\n// Index: 5\tCharacter: l\t\tCode Point: U+006C\n// Index: 6\tCharacter: l\t\tCode Point: U+006C\n// Index: 7\tCharacter: o\t\tCode Point: U+006F",
      "lang": "go"
     },
     {
      "title": "Interface as a set of methods",
      "body": "In its simplest form, an **interface type** is a set of method signatures.\nHere is an example of an interface definition that includes two methods `Add` and `Value`:\n\nThe parameter names like `increment` can be omitted from the interface definition but they often increase readability.\n\nInterface names in Go do not contain the word `Interface` or `I`.\nInstead, they often end with `er`, e.g. `Reader`, `Stringer`.",
      "code": "type Counter interface {\n    Add(increment int)\n    Value() int\n}",
      "lang": "go"
     },
     {
      "title": "Implementing an interface",
      "body": "Any type that defines the methods of the interface automatically implicitly \"implements\" the interface.\nThere is no `implements` keyword in Go.\n\nThe following type implements the `Counter` interface we saw above.\n\nFor implementing the interface, it does not matter whether the method has a value or pointer receiver.\n(Revisit the methods concepts if you are unsure about those.)\nA value of interface type can hold any value that implements those methods. [^1]\n\nThat means `Stats` can now be used in all the places that expect the `Counter` interface.\n\nBecause interfaces are implemented implicitly, a type can easily implement multiple interfaces.\nIt only needs to have all the necessary methods defined.",
      "code": "type Stats struct {\n    value int\n    // ...\n}\n\nfunc (s Stats) Add(v int) {\n    s.value += v\n}\n\nfunc (s Stats) Value() int {\n    return s.value\n}\n\nfunc (s Stats) SomeOtherMethod() {\n    // The type can have additional methods not mentioned in the interface.\n}",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "logs-logs-logs: tests not parsed (no extractor for 'go')",
    "airport-robot: tests not parsed (no extractor for 'go')",
    "sorting-room: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 8,
   "name": "The Warded Gate",
   "concepts": [
    "errors",
    "floating-point-numbers",
    "zero-values"
   ],
   "exercises": [
    "the-farm",
    "census",
    "interest-is-interesting"
   ],
   "lesson": {
    "sections": [
     {
      "title": "The error interface",
      "body": "Error handling is **not** done via exceptions in Go.\nInstead, errors are normal **values** of types that implement the built-in `error` interface.\nThe `error` interface is very minimal.\nIt contains only one method `Error()` that returns the error message as a string.\n\nEvery time you define a function in which an error could happen during the execution that needs to reach the caller, you need to include `error` as one of the return types.\nIf the function has multiple return values, by convention `error` is always the last one.",
      "code": "type error interface {\n  Error() string\n}",
      "lang": "go"
     },
     {
      "title": "Creating and returning a simple error",
      "body": "You do not have to always implement the error interface yourself.\nTo create a simple error, you can use the `errors.New()` function that is part of the standard library package `errors`.\nThe only thing you need to pass in is the error message as a string, and `errors.New()` will take care of creating a value that contains your message and implements the `error` interface.\n\nIf the function returns an error, it is good practice to return the zero value for all other return parameters:\n\nYou should not assume that all functions return zero values for other return values if an error is present.\nIt is best practice to assume that it is not safe to use any of the other return values if an error occurred.\nThe only exceptions are cases where the documentation clearly states that other returns values are meaningful in case of an error.\n\nIf you want to use such a simple error in multiple places, you should declare a variable for the error instead of using `errors.New` in-line.\nBy convention, the name of the variable should start with `Err` or `err` (depending on whether it is exported or not).\nThese error variables are often called **sentinel errors**.\n\nReturn `nil` for the error to signal that there were no errors during the function execution:",
      "code": "func DoSomething() (SomeStruct, int, error) {\n  // ...\n  return SomeStruct{}, 0, errors.New(\"failed to calculate result\")\n}",
      "lang": "go"
     },
     {
      "title": "Go does not have a concept of empty, null, or undefined...",
      "body": "Go does not have a concept of empty, null, or undefined for variable values.\nVariables declared without an explicit initial value default to the zero value for their respective type.\n\nThe zero value for primitive types such as booleans, numeric types, and strings are `false`, `0`, and `\"\"`, respectively.\n\nThe identifier `nil`, meaning zero, is the zero value for more complex types such as pointers, functions, interfaces, slices, channels, and maps.\n\nThe following table details the zero value for Go's types.\n\nYou may have noticed struct types are absent from the above table.\nThat is because the zero value for a struct type depends on its fields.\nStructs are set to their zero value when all of its fields are set to their respective zero value.",
      "code": "// Package census simulates a system used to collect census data.\npackage census\n\n// Resident represents a resident in this city.\ntype Resident struct {\n\tName    string\n\tAge     int\n\tAddress map[string]string\n}\n\n// NewResident registers a new resident in this city.\nfunc NewResident(name string, age int, address map[string]string) *Resident {\n\treturn &Resident{\n\t\tName:    name,\n\t\tAge:     age,\n\t\tAd",
      "lang": "go"
     },
     {
      "title": "A floating-point number is a number with zero or more...",
      "body": "A floating-point number is a number with zero or more digits behind the decimal separator.\nExamples are `-2.4`, `0.1`, `3.14`, `5.`, `16.984025` and `1024.0`.\n\nDifferent floating-point types can store different numbers of digits after the digit separator - this is referred to as its precision.\n\nGo has two floating-point types:\n\n- `float32`: 32 bits (~6-9 digits precision).\n- `float64`: 64 bits (~15-17 digits precision).\n   This is the default floating-point type.\n\nAs can be seen, both types can store a different number of digits.\nThis means that trying to store PI in a `float32` will only store the first 6 to 9 digits (with the last digit being rounded).\n\nBy default, Go will use `float64` for floating-point numbers, unless the floating-point number is:\n\n1. assigned to a variable with type `float32`, or\n2. returned from a function with return type `float32`, or\n3. passed as an argument to the `float32()` function.\n\nThe [`math` package] contains many helpful mathematical functions.",
      "code": "package interest\n\n// InterestRate returns the interest rate for the provided balance.\nfunc InterestRate(balance float64) float32 {\n\tif balance < 0.0 {\n\t\treturn 3.213\n\t}\n\n\tif balance < 1000.0 {\n\t\treturn 0.5\n\t}\n\n\tif balance < 5000.0 {\n\t\treturn 1.621\n\t}\n\n\treturn 2.475\n}\n\n// Interest calculates the interest for the provided balance.\nfunc Interest(balance float64) float64 {\n\treturn balance * (float64(I",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "the-farm: tests not parsed (no extractor for 'go')",
    "census: tests not parsed (no extractor for 'go')",
    "interest-is-interesting: tests not parsed (no extractor for 'go')"
   ]
  },
  {
   "n": 9,
   "name": "Depths of Abstraction",
   "concepts": [
    "first-class-functions",
    "randomness",
    "regular-expressions",
    "stringers"
   ],
   "exercises": [
    "expenses",
    "animal-magic",
    "parsing-log-files",
    "meteorology"
   ],
   "lesson": {
    "sections": [
     {
      "title": "Function types",
      "body": "Using function values is possible thanks to the function types in Go.\nA function type denotes the set of all functions with the same sequence of parameter types and the same sequence of result types.\nUser-defined types can be declared on top of function types.\nFor instance, the `dialog` function from the previous examples can be updated as following:",
      "code": "type greetingFunc func(string) string\n\nfunc dialog(name string, f greetingFunc) {\n\tfmt.Println(f(name))\n\tfmt.Println(\"I'm a dialog bot.\")\n}",
      "lang": "go"
     },
     {
      "title": "Anonymous functions",
      "body": "Another powerful tool that is available thanks to first-class functions support is anonymous functions.\nAnonymous functions are defined at their point of use, without a name following the `func` keyword.\nSuch functions have access to the variables of the enclosing function.\n\nFor example:\n\nA call to `fib` declares the variables `n1` and `n2` and returns an anonymous function that, in turn, changes the values of these variables each time the function is called.\nNth calls of the anonymous function return the Nth number of the Fibonacci sequence starting from 0. The anonymous inner function has access to the local variables (`n1` and `n2`) of the enclosing function `fib`.\nThis is a great way to have function values keep state between calls.\nWe say that the anonymous function is a closure of the variables `n1` and `n2`. Closures are widely used in programming and you might see other languages supporting them.",
      "code": "func fib() func() int {\n\tvar n1, n2 int\n\n\treturn func() int {\n\t\tif n1 == 0 && n2 == 0 {\n\t\t\tn1 = 1\n\t\t} else {\n\t\t\tn1, n2 = n2, n1 + n2\n\t\t}\n\t\treturn n2\n\t}\n}\n\nnext := fib()\nfor i := range N {\n  fmt.Printf(\"F%d\\t= %4d\\n\", i, next())\n}",
      "lang": "go"
     },
     {
      "title": "Seeds",
      "body": "The number sequences generated by package `math/rand` are not truly random.\nGiven a specific \"seed\" value, the results are entirely deterministic.\n\nIn Go 1.20+ the seed is automatically picked at random so you will see a different sequence of random numbers each time you run your program.\n\nIn prior versions of Go, the seed was `1` by default.\nSo to get different sequences for various runs of the program, you had to manually seed the random number generator, e.g. with the current time, before retrieving any random numbers.",
      "code": "rand.Seed(time.Now().UnixNano())",
      "lang": "go"
     },
     {
      "title": "Syntax",
      "body": "The syntax of the regular expressions accepted is the same general syntax used by Perl, Python, and other languages.\n\nBoth the search patterns and the input texts are interpreted as UTF-8.\n\nWhen using backticks (\\`) to make strings, backslashes (`\\`)  don't have any special meaning and don't mark the beginning of special characters like tabs `\\t` or newlines `\\n`:\n\nBecause of this, using backticks is desirable to make regular expressions,\nbecause it means we don't need to escape backslashes:",
      "code": "\"\\t\\n\" // regular string literal with 2 characters: a tab and a newline\n`\\t\\n`// raw string literal with 4 characters: two backslashes, a 't', and an 'n'",
      "lang": "go"
     }
    ]
   },
   "practice": [],
   "exam": [],
   "_todo": [
    "practice needs 6 more challenge(s)",
    "exam: author 8-12 questions",
    "expenses: tests not parsed (no extractor for 'go')",
    "animal-magic: tests not parsed (no extractor for 'go')",
    "parsing-log-files: tests not parsed (no extractor for 'go')",
    "meteorology: tests not parsed (no extractor for 'go')"
   ]
  }
 ]
}